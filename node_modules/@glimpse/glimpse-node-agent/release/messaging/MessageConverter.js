'use strict';
var GuidHelper_1 = require('./../util/GuidHelper');
var MessageConverter = (function () {
    function MessageConverter() {
    }
    MessageConverter.convertMessage = function (data, types, indices, context) {
        var message = {
            id: GuidHelper_1.GuidHelper.newGuid(false),
            context: {
                id: context.id,
                type: context.type
            },
            ordinal: ++MessageConverter.i,
            types: types,
            indices: indices,
            payload: undefined
        };
        var payload = {
            id: message.id,
            payload: data,
            ordinal: message.ordinal,
            context: message.context,
            types: types
        };
        message.payload = JSON.stringify(payload);
        return message;
    };
    ;
    MessageConverter.i = 0;
    return MessageConverter;
}());
exports.MessageConverter = MessageConverter;

//# sourceMappingURL=../../maps/messaging/MessageConverter.js.map
