'use strict';

var bodyParser = require('body-parser');
var express = require('express');
var logger = require('morgan');
var cors = require('cors');
var router = express.Router();

// Tell Glimpse to ignore this router (and its middleware)...
router.glimpse = {
    ignore: true
};

var server = require('../Server');

var resourceRuntimeModule = require('../resources/ResourceRuntime');
var resourceRuntime = new resourceRuntimeModule.ResourceRuntime(server.instance);

// TODO: need to add "auth" to dictate when Glimpse endpoint should be accessable vs not
router.use(logger('dev'));
router.use(cors());
router.use(bodyParser.json({ type: ['application/json', 'application/x-www-form-urlencoded'] }));

resourceRuntime.register();
router.use(function callback() {
    resourceRuntime.processRequest.apply(resourceRuntime, arguments);
});

module.exports = router;
//# sourceMappingURL=../../maps/routes/glimpse.js.map
