'use strict';
var Resource = (function () {
    function Resource(server) {
        this.name = 'export-config';
        this.uriTemplate = '?hash={hash}';
        this.type = 'client';
        this.resourceManager = server.providers.resourceManager;
    }
    Resource.prototype.invoke = function (req, res) {
        var baseUri = req.protocol + '://' + req.get('host') + '/glimpse';
        var resources = this.resourceManager.getUriTemplates(baseUri);
        res.setHeader('Content-Disposition', 'attachment; filename = glimpse.json');
        res.json(resources);
    };
    ;
    return Resource;
}());
exports.Resource = Resource;

//# sourceMappingURL=../../maps/resources/ExportConfigResource.js.map
