'use strict';

var server = require('./Server');
var host = require('./hosting/host-express');

var init = function init() {
    // setup hosting intercept
    host();
};

// TODO: Move this to server type once hosting has been converted to TypeScript.
server.instance.init = init;

module.exports.server = server.instance;
